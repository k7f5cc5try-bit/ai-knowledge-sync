# v0.1.5 验证记录

本次加入用户 RAG few-shot，易混淆改为简短对比，相关笔记独立且最多 3 个、目标未确认则省略。示例同步为汇总笔记适用的 #### 层级。脚本未改动；检查本地引用及示例保存，不声称模型在所有宿主均遵守格式。

本次仅更新一句话安装、语义识别及仓库首页指引；执行脚本未改动。检查包内链接与目录完整性；未验证每个宿主是否自动读取 GitHub 首页。

本轮针对真实反馈修正：内容优先突出核心能力；新增 destination_kind 和 --require-obsidian，阻止测试/未配置目的地冒充正式 Obsidian。20 项测试通过，包括测试目录存在模拟 .obsidian 时仍拒绝正式保存。默认示例配置现在为 unconfigured，旧配置需要明确迁移。Codex v0.1.2 的本地技能发现、脚本保存、图片导出已测试；不据此宣称所有 agent 的自动触发都通过。

本次更新：增加六类 agent 的安装指南及不可用原因；笔记增加“总结”和仅列名称的“易混淆的概念”；新增只读双链条件与目标检查脚本；标题改为纯文本，避免未验证的空链接。六项内容质量由 agent 判断，保存脚本不作语义验收。

验证日期：2026-09-10。

- Windows，Python 3.13.2：15 项 unittest 测试通过。
- 新增覆盖非 Obsidian 环境、唯一同名文件、目标缺失、重名、禁用双链、汇总笔记概念标题、忽略代码块中的假标题、重复标题、仓库范围和不再生成未验证标题双链。测试均使用临时目录，无真实私人笔记。
- 覆盖新增笔记、重复执行去重、相同 ID 内容冲突、原历史字节与编号保持、缺失/重复锚点拒绝写入、锁阻止并发脚本、配置相对路径、拒绝外部 SVG 资源及未解析变量、转换失败不发布旧图、无效 PNG 拒绝。
- CLI 实际保存示例正文成功。
- v0.1.0 曾使用本机浏览器实际将示例 SVG 导出为 1360 × 580 PNG；人工视觉检查文字清楚，无明显裁切。本次未改动渲染逻辑，未重复浏览器转换。沙箱内浏览器启动失败，允许沙箱外运行后通过；宿主需允许本地浏览器执行。
- SKILL.md 已检查 name、description、license 及本地引用。随宿主提供的 quick_validate.py 因环境缺少 PyYAML 未运行完成，不将其列为通过；本项目执行器本身不依赖 PyYAML。
- 已按官方网页核对 INSTALL.md 中的平台安装入口、目录和调用方法，来源链接已放在对应段落；这不等于已在这些应用中实际安装运行。
- 未验证：Linux/macOS、Python 3.10 实机、WorkBuddy 或其他宿主自动触发的端到端行为、Obsidian 中实际点击跳转、与 Obsidian/云同步并发写入、中文字体复杂配图。

重新验证：在项目根目录执行 `python -m unittest discover -s tests -v`，再按 README 的 CLI 示例实际保存和转换，并查看输出图片。
